# Both frontend and backend use a real node_modules tree (NOT Plug'n'Play) so Prisma,
# Jest/Testcontainers, bundlers, and editor tooling work without PnP loader shims.
# See CLAUDE.md.
nodeLinker: node-modules
