{
  "name": "artifact-hub",
  "private": true,
  "version": "0.0.0",
  "packageManager": "yarn@4.5.1",
  "engines": {
    "node": ">=24"
  },
  "volta": {
    "node": "24.19.0",
    "yarn": "4.5.1"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "yarn workspace contracts build && yarn workspace backend build && yarn workspace frontend build",
    "test": "yarn workspace backend test && yarn workspace frontend test",
    "lint": "yarn workspace contracts lint && yarn workspace backend lint && yarn workspace frontend lint",
    "typecheck": "yarn workspace contracts typecheck && yarn workspace backend typecheck && yarn workspace frontend typecheck",
    "dev:services": "docker compose up -d"
  },
  "devDependencies": {
    "@eslint/js": "^9.17.0",
    "eslint": "^9.17.0",
    "typescript": "^5.7.2",
    "typescript-eslint": "^8.18.1"
  }
}
